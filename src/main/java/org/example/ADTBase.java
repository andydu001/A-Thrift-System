package org.example;

import org.apache.thrift.TBaseProcessor;

import java.util.Map;

public class ADTBase extends TBaseProcessor {
    protected ADTBase(Object iface, Map map) {
        super(iface, map);

    }
}
